export const environment = {
  production: false,
  apiBaseUrl: 'https://localhost:7016',

  // Empty locally, so a dev run draws routes on a blank map. Paste a key in to see tiles while
  // working on the map itself — and do not commit it (#193).
  mapApiKey: '',
};
